#ifndef MAIN_H
#define MAIN_H

/* Function declarations */
int _putchar(char c);
int _printf(char *s, ...);
int handle_f_specifier(char specifier, va_list args);

/* Header files*/
#include <stdarg.h>

#endif